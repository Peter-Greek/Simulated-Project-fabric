package dev.simulated_team.simulated.backport.physics.neoforge.mixinhelper.compatibility.flywheel;

/**
 * Sable's Flywheel embedding, which lets a sub-level's blocks be instanced.
 * Inert here: nothing is embedded, so Flywheel draws the world as usual.
 */
public interface SubLevelEmbedding {
}
