package dev.simulated_team.simulated.backport.physics.mixinterface.clip_overwrite;

/**
 * Sable mixes this onto the clip context so a ray can be traced in a
 * sub-level's frame. Inert: every trace is already in world space.
 */
public interface LevelPoseProviderExtension {
}
