package dev.simulated_team.simulated.backport.physics.sublevel.storage;

/** Why a sub-level went away. Declared for upstream signatures. */
public enum SubLevelRemovalReason {
    UNLOADED,
    DISASSEMBLED,
    DELETED
}
