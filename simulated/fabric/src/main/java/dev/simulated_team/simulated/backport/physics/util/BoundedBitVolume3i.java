package dev.simulated_team.simulated.backport.physics.util;

/** A packed bit set over a block volume. Never populated here. */
public class BoundedBitVolume3i {

    /** The volume covering these blocks. Nothing reads it while there is no solver. */
    @javax.annotation.Nullable
    public static BoundedBitVolume3i fromBlocks(final java.util.Collection<? extends net.minecraft.core.BlockPos> blocks) {
        return null;
    }
}
