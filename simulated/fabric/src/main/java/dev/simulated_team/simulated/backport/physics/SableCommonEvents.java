package dev.simulated_team.simulated.backport.physics;

/**
 * Sable's own event bus. Nothing fires here, so a registered listener simply
 * never runs.
 */
public final class SableCommonEvents {

    private SableCommonEvents() {
    }
}
